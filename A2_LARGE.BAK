//program to largest of three numbers

#include "conio.h"
#include "iostream.h"

class find
{
	int x1, x2, x3;
public:
	void get_data()
	{
		cout << "Enter first numbers: ";
		cin >> x1;
		cout << "Enter second numbers: ";
		cin >> x2;
		cout << "Enter third numbers: ";
		cin >> x3;
	}
	void largest()
	{
		if ((x1>x2) && (x1>x3))
			cout << "\nFirst number "<<x1<<" is largest";
		if ((x2>x1) && (x2>x3))
			cout << "\nSecond number "<<x2<<" is largest";
		else
			cout << "\nThird number "<<x3<<" is largest";
	}
};
int main(int argc, char *argv)
{
	clrscr();
	class find f1;
	f1.get_data();
	f1.largest();
	getch();
	return 0;
}
